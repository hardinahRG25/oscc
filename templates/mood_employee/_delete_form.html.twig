<form method="post" action="{{ path('app_mood_employee_delete', {'id': mood_employee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mood_employee.id) }}">
    <button class="btn">Delete</button>
</form>
