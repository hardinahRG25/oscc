<form method="post" action="{{ path('app_leave_company_delete', {'id': leave_company.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ leave_company.id) }}">
    <button class="btn">Delete</button>
</form>
