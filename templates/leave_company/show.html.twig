{% extends 'base.html.twig' %}

{% block title %}LeaveCompany{% endblock %}

{% block body %}
    <h1>LeaveCompany</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ leave_company.id }}</td>
            </tr>
            <tr>
                <th>Date_resignation</th>
                <td>{{ leave_company.dateResignation ? leave_company.dateResignation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Reason_resignation</th>
                <td>{{ leave_company.reasonResignation }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ leave_company.createdAt ? leave_company.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_leave_company_index') }}">back to list</a>

    <a href="{{ path('app_leave_company_edit', {'id': leave_company.id}) }}">edit</a>

    {{ include('leave_company/_delete_form.html.twig') }}
{% endblock %}
