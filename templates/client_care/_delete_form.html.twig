<form method="post" action="{{ path('app_client_care_delete', {'id': customer_care.id}) }}" onsubmit="return confirm('Êtes-vous sûr de supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customer_care.id) }}">
    <button class="btn">Delete</button>
</form>
